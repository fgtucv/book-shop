{
  "name": "book-shop",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "homepage": "https://fgtucv.github.io/book-shop/",
  "scripts": {
    "start": "parcel src/index.html src/basket.html --no-cache",
    "build": "parcel build src/index.html src/basket.html --no-cache --public-url /book-shop/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@parcel/transformer-sass": "^2.15.1",
    "gh-pages": "^6.3.0",
    "parcel": "^2.15.1",
    "parcel-reporter-clean-dist": "^1.0.4",
    "sass": "^1.89.2"
  },
  "devDependencies": {
    "posthtml-include": "^2.0.1",
    "svgo": "^3.3.2"
  }
}
